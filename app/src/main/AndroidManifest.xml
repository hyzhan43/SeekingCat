<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="zqx.rj.com.seekingcat">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name="zqx.rj.com.base.app.BaseApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".account.ui.LoginActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".publish.ui.activity.PublishActivity" />
        <activity android:name=".mine.ui.activity.AboutActivity" />
        <activity android:name=".home.ui.activity.GoodsDetailActivity" />
        <activity android:name=".common.search.ui.activity.SearchActivity" />
        <activity android:name=".mine.ui.activity.MyPublishActivity" />
        <activity android:name=".mine.ui.activity.FollowActivity" />
        <activity android:name=".account.ui.RegisterActivity" />
    </application>

</manifest>